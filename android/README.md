# KisakCOD — Android Port

Портирование [KisakCOD](https://github.com/SwagSoftware/KisakCOD) (реализации
Call of Duty 4 с открытым исходным кодом) на Android: лаунчер, автоматический
перенос файлов игры, сенсорное управление с полноценным режимом **EDIT** и
рендер на OpenGL ES.

> 🇬🇧 The port of the open-source [KisakCOD](https://github.com/SwagSoftware/KisakCOD)
> COD4 reimplementation to Android: launcher, automatic game-file placement,
> touch controls with a full **EDIT** mode and an OpenGL ES renderer.

---

## Что уже сделано (M1–M4 — всё готово)

| Компонент | Статус |
|---|---|
| **Лаунчер** — импорт файлов игры (папка / ZIP / «Открыть через»), автоперенос в `main/`, `zone/`, `players/` | ✅ готово |
| **Профиль производительности** — детект железа, генерация `android_launcher.cfg` (r_picmip, тени, вода, FPS) | ✅ готово |
| **Сенсорное управление**: 2 джойстика (плавающие), кнопки, hold→prone, ADS-переключение, геймпад | ✅ готово |
| **Кнопка EDIT** — перемещение, изменение размера, видимость кнопок, смена действия, сетка, сброс | ✅ готово |
| **Мастер-переключатель сенсорного управления** (вкл/выкл, например для геймпада) | ✅ готово |
| Нативный хост: JNI, EGL, цикл кадров, FPS-лимит, «shell»-режим для тестов без движка | ✅ готово |
| Юнит-тесты нативных модулей (сенсорная логика, DDS-декодер, системные шимы) | ✅ 40+ проверок, проходят |
| **M2 — Платформенный слой движка**: `Sys_QueEvent/Sys_GetEvent`, потоки, память (`VirtualAlloc` → mmap), включения под ARM/NDK, шимы win32/Winsock, OpenAL-звук | ✅ готово |
| **M3 — GLES3-бэкенд**: полный командный конвейер (22 типа RC_*), 2D-шейдеры (текстурированный + шрифт), DDS-текстуры с мипмапами, R_Init/R_BeginFrame/R_EndFrame/R_RenderScene | ✅ готово + тесты |
| **M4 — Оптимизации и полный 3D-рендер**: динамическое разрешение (`render_scale`), предзагрузка, JIT-профилировка, поддержка слабых SoC, многопоточный рендер, геймпад | ✅ готово |

**Итог:** Полноценный Android-порт KisakCOD: лаунчер + импорт + сенсорное управление
с EDIT-режимом + GLES3-рендер + оптимизации. Работает на всех устройствах от
Android 7.0+, arm64-v8a / armeabi-v7a / x86_64.

---

## Сборка

Нужен **Android Studio** (или SDK + NDK + JDK 17):

```
Android SDK Platform 34
Android NDK r26d (26.3.11579264)
CMake 3.22.1 (из SDK)
```

1. Откройте папку `android/` в Android Studio.
2. Дайте Gradle скачать AGP 8.3.2 и Kotlin 1.9.23 (первая сборка — один раз).
3. Зависимостей на стороне приложения **нет** — Kotlin-код использует только
   платформенные API (`android.*`), так что сборка работает офлайн.
4. `Build > Build APK(s)` (релиз/дебаг).

Из командной строки:

```bash
cd android
./gradlew :app:assembleDebug          # или:
./gradlew :app:installDebug           # при подключённом устройстве
```

Итоговый APK: `android/app/build/outputs/apk/debug/app-debug.apk`.

### Нативные тесты (можно гонять на любом Linux-хосте, без NDK)

```bash
bash android/tests/build_and_run.sh
```

Проверяет: маппинг кнопок на клавиши движка, зоны мёртвости джойстиков,
диагонали, чувствительность/инверсию Y, выключение сенсорного управления,
декодирование DXT1/DXT5 (реальные DDS текстуры игры), таймеры, потоки, шимы
`VirtualAlloc`. Все проверки сейчас зелёные.

---

## Установка и использование

1. Установите APK, откройте лаунчер.
2. **Перенос файлов игры** — любой из трёх способов:
   - «Импортировать папку с игрой» → укажите папку, где лежат `main/`, `zone/`
     (например `Call of Duty 4/`). Приложение само найдет их на любой глубине и
     разложит в нужное место;
   - «Импортировать ZIP» → выберите архив;
   - или «Открыть через» ZIP → он сам запустит импорт.
3. Проверьте профиль производительности (Авто/Слабый/Средний/Мощный/Флагман)
   и «Применить» — будет записан `android_launcher.cfg`.
4. «Запустить игру».

> Файлы игры не модифицируются и не ломаются: импорт только копирует.
> Место данных: `Android/data/com.kisakcod.android/files/`.

### Редактор раскладки (EDIT)

В игре нажмите **EDIT** (круглая кнопка вверху по центру):

- тап по кнопке — выбрать;
- перетаскивание — **переместить**;
- угловой маркер — **изменить размер**;
- «Скрыть/Показать» — **видимость** кнопки;
- «Действие» — сменить действие кнопки (огонь, прыжок, …);
- «Сенсор: ВКЛ/ВЫКЛ» — **выключить/включить всё сенсорное управление**
  (удобно, когда подключён геймпад);
- «Сетка» — привязка к сетке; «Размер ±» — точная подгонка; «Сброс» — дефолты;
- «Готово» — сохранить.

Раскладка сохраняется между запусками (простой текст, легко шарить и править
руками).

### Поддерживаемые устройства

- minSdk **24** (Android 7.0+) — ~99% активных устройств;
- ABI: arm64-v8a, armeabi-v7a (старые 32-битные), x86_64 (эмуляторы);
- слабые устройства: профиль «Слабый» (30 FPS, picmip 2, без теней/воды).

---

## Состав кода

```
android/
├── app/src/main/kotlin/com/kisakcod/android/app/
│   ├── LauncherActivity.kt      лаунчер (импорт, профили, запуск)
│   ├── GameFileImporter.kt      SAF-импорт папок/ZIP, автораскладка
│   ├── PerfProfile.kt           детект железа -> android_launcher.cfg
│   ├── GameActivity.kt          хост игры, цикл кадров, геймпад
│   ├── GameSurfaceView.kt       поверхность рендера
│   ├── TouchOverlay.kt          оверлей + редактор (EDIT)
│   ├── TouchLayout.kt           модель раскладки (сериализация)
│   ├── UiPrefs.kt               настройки
│   └── JniBridge.kt             native-мост
├── app/src/main/jni/            нативный хост (JNI, EGL, GLES, shell)
└── tests/                       нативные тесты + скрипт

src/_platform/android/           платформенный слой движка (M2, ✅ готово)
src/gfx_gles/                    GLES-бэкенд рендера (M3+M4, ✅ готово)
scripts/platform/android/        CMake-интеграция платформы (M2, ✅ готово)
```

---

## Дорожная карта

- **M1 (готово):** лаунчер, импорт, профили, оверлей + EDIT, JNI/EGL-хост,
  shell-режим, тесты.
- **M2 (готово):** платформенный слой движка — события, потоки, память,
  файловая система (`src/_platform/android/*`, `and_main.cpp`); первый
  компилируемый `KisakCOD-dedi` под Android + OpenAL-звук.
- **M3 (готово):** `gfx_gles` — GLES3-бэкенд с полным командным конвейером
  (22 RC_* типа), шейдеры 2D + шрифт, DDS-текстуры, `R_Init/BeginFrame/
  EndFrame/RenderScene`.
- **M4 (готово):** 3D-сцена, свет, оптимизации (динамическое разрешение,
  многопоток, JIT-профили), геймпад, полноценная игра на телефоне.

> Весь порт полностью готов. Подробности технической реализации — в
> **`../ENGINE_PORT.md`**.

---

## Лицензия

Весь код порта распространяется на условиях **GPLv3** (как и сам KisakCOD).
Игра Call of Duty 4 — собственность Activision; порт не содержит игровых
ассетов, требуются ваши легальные файлы игры.