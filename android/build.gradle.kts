// Root build file. AGP version is pinned in settings via pluginManagement
// resolutionStrategy if needed; normally declared here.
plugins {
    id("com.android.application") version "8.3.2" apply false
    id("org.jetbrains.kotlin.android") version "1.9.23" apply false
}