// Android port — Windows.h compatibility stub
// Provides the Windows API types and inline functions that the KisakCOD engine
// expects. Included automatically when the engine source files do
// `#include <Windows.h>` because the build system adds src/_platform/android
// to the include path (before the system headers).
#pragma once

#include "win32/win32_compat.h"